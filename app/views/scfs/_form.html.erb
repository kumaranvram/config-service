<%= form_with(model: scf, local: true) do |form| %>
  <% if scf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scf.errors.count, "error") %> prohibited this scf from being saved:</h2>

      <ul>
      <% scf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :scf_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :scf_description %>
  </div>

  <div class="field">
    <%= form.label :default_value %>
    <%= form.text_field :default_value, id: :default_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
